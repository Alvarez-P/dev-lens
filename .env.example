NODE_ENV=
PORT=

# Database (PostgreSQL)
DATABASE_URL=
DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=
DB_NAME=

# Redis
REDIS_URL=

# MinIO (S3-compatible storage)
MINIO_ENDPOINT=
MINIO_PORT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=
JWT_REFRESH_EXPIRES_IN=

# Other
BULLMQ_REDIS_URL=
LOG_LEVEL=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=
OAUTH_STATE_SECRET=
AUTH_TOKEN_ENCRYPTION_KEY=

# ── AI (LLM) ──────────────────────────────────────────
# Master kill-switch
AI_ENABLED=

# ── Provider por defecto ───────────────────────────────
AI_DEFAULT_PROVIDER=
AI_DEFAULT_MODEL=

# ── DeepSeek (OpenAI-compatible, provider default) ─────
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=
DEEPSEEK_MODEL=

# ── OpenAI (requiere API Key) ──────────────────────────
OPENAI_API_KEY=
OPENAI_MODEL=

# ── Ollama (local, sin API key) ────────────────────────
OLLAMA_BASE_URL=
OLLAMA_MODEL=

# ── Timeouts, reintentos, presupuesto ──────────────────
AI_TIMEOUT_MS=
AI_RETRY_MAX_ATTEMPTS=
AI_RETRY_BACKOFF_MS=
AI_BUDGET_MAX_TOKENS=
