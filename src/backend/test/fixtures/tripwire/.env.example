# Deny-list tripwire fixture — FAKE secrets only, never a real credential.
# The evaluation harness asserts this file never reaches any LLM prompt.
DB_HOST=localhost
DB_PORT=5432
TRIPWIRE_SECRET=sk-tripwire-super-secret-9f3a1c
